---
import BestyrelseCard from "../components/BestyrelseCard.astro";
import MainLayout from "../layouts/MainLayout.astro";
const valbyIfBoard = await fetch(
  "https://efrjvsgbrgktorrxkiyp.supabase.co/rest/v1/valby_if",
  {
    method: "GET",
    headers: {
      apikey:
        "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVmcmp2c2dicmdrdG9ycnhraXlwIiwicm9sZSI6ImFub24iLCJpYXQiOjE2OTYzNDgxOTUsImV4cCI6MjAxMTkyNDE5NX0.FZgFg8q1LZylUrKUOvO7aIbWt83tY4dKVuVyAKKgxVU",
    },
  }
).then((res) => res.json());
---

<MainLayout>

    <main>
      <h1>Bestyrelsen</h1>

      <section>
        {
          valbyIfBoard.map((data) => {
            return (
              <BestyrelseCard
                img_url={data.board_member_img_url}
                board_member_name={data.board_member_name}
                board_member_title={data.board_member_title}
                board_member_date={data.board_member_date}
                board_member_info1={data.board_member_info1}
                board_member_info2={data.board_member_info2}
                board_member_info3={data.board_member_info3}
                board_member_info4={data.board_member_info4}
                board_member_responsibilities={
                  data.board_member_responsibilities
                }
              ></BestyrelseCard>
            );
          })
        };
      </section>
    </main>

  </MainLayout>

<style>
  main {
    margin-top: 160px;
  }

  section {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    min-height: 100vh;
  }

  body {
    background-color: #dededf;
  }

  h1 {
    text-align: center;
    text-decoration: underline;
    margin-bottom: 20px;
  }

  @media screen and (min-width: 768px) {
    /* Styles for tablets and desktop */

    .bestyrelse_card {
      display: flex;
      flex-direction: row;
      width: auto;
    }

    img {
      width: 400px;
      align-items: center;
    }

    p {
      max-width: 300px;
    }
  }
</style>
