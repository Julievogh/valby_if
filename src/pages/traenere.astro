---
import TraenerCard from "../components/TraenerCard.astro";
import MainLayout from "../layouts/MainLayout.astro";

const valbyIfCoaches = await fetch(
  "https://efrjvsgbrgktorrxkiyp.supabase.co/rest/v1/valby_if_coaches",
  {
    method: "GET",
    headers: {
      apikey:
        "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVmcmp2c2dicmdrdG9ycnhraXlwIiwicm9sZSI6ImFub24iLCJpYXQiOjE2OTYzNDgxOTUsImV4cCI6MjAxMTkyNDE5NX0.FZgFg8q1LZylUrKUOvO7aIbWt83tY4dKVuVyAKKgxVU",
    },
  }
).then((res) => res.json());
---

<MainLayout>
    <main>
      <h1>Trænere</h1>

      <section>
        {
          valbyIfCoaches.map((data) => {
            return (
              <TraenerCard
                coach_image_url={data.coach_image_url}
                coach_name={data.coach_name}
                coach_date={data.coach_date}
                coach_info1={data.coach_info1}
                coach_info2={data.coach_info2}
                coach_info3={data.coach_info3}
                coach_info4={data.coach_info4}
              />
            );
          })
        }
      </section>
    </main>
  </MainLayout>

<style>
  main {
    margin-top: 160px;
  }

  section {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    min-height: 100vh;
  }

  body {
    background-color: #dededf;
  }

  h1 {
    text-align: center;
    text-decoration: underline;
    margin-bottom: 20px;
  }

  @media screen and (min-width: 768px) {
    /* Styles for tablets and desktop */

    .bestyrelse_card {
      display: flex;
      flex-direction: row;
      width: auto;
    }

    img {
      width: 400px;
      align-items: center;
    }

    p {
      max-width: 300px;
    }
  }
</style>
